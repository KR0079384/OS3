# Frontend Services

This directory contains HTTP API client abstractions for communicating with the backend FastAPI services.

## Files

| File | Description |
|---|---|
| `api.ts` | Axios / fetch API client for invoking backend endpoints (`/api/scan-package`, `/api/copilot/ask`). |
