# Frontend React Hooks

This directory contains custom React hooks used across frontend components for UI state management.

## Files

| File | Description |
|---|---|
| `use-mobile.tsx` | Custom hook for tracking mobile viewport screen width matching. |
| `use-toast.ts` | Custom hook for managing toast notification state and dispatching toast events. |
