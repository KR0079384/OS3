# Frontend Libraries & Utilities

This directory contains third-party SDK configuration (Firebase) and utility helper functions.

## Files

| File | Description |
|---|---|
| `firebase.ts` | Initializes Firebase app client, Authentication SDK, and Firestore database services. |
| `utils.ts` | Exports `cn()` helper function combining `clsx` and `tailwind-merge` for dynamic CSS class strings. |
